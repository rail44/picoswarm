# picoswarm

A lightweight orchestrator for running multiple Claude Code and similar CLI coding agents in parallel.

The CLI binary is `pswarm`. The project name is `picoswarm`.

Status: MVP. See `CLAUDE.md` for the project's direction and `docs/plan.md` for the current scope and roadmap.
