//! picoswarm internals exposed as a library so integration tests can
//! use the same protocol types and helpers as the binary.

pub mod cli;
pub mod client;
pub mod daemon;
pub mod paths;
pub mod protocol;
