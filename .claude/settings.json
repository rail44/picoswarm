{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r \".tool_input.file_path // empty\"); case \"$file\" in *.rs) if grep -q \"#\\[test\\]\" \"$file\" 2>/dev/null; then cd \"$CLAUDE_PROJECT_DIR\" && cargo test --quiet 2>&1 | tail -50; else cd \"$CLAUDE_PROJECT_DIR\" && cargo check --quiet 2>&1 | tail -30; fi ;; esac",
            "timeout": 60,
            "statusMessage": "cargo verifying"
          }
        ]
      }
    ]
  }
}
