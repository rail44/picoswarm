{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r \".tool_input.file_path // empty\"); case \"$file\" in *.rs) cd \"$CLAUDE_PROJECT_DIR\" && cargo fmt && cargo clippy --all-targets --quiet -- -D warnings 2>&1 | tail -30 ;; esac",
            "timeout": 60,
            "statusMessage": "cargo fmt + clippy"
          }
        ]
      }
    ]
  }
}
